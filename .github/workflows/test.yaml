name: Test .NET solution or project
permissions:
  packages: read
on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    uses: devantler-tech/reusable-workflows/.github/workflows/ci-dotnet-test.yaml@5acd692154f7c4457bbb84636ac366074ddd6273 # v1.19.7
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
