name: Publish
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  packages: write

jobs:
  publish:
    uses: devantler-tech/reusable-workflows/.github/workflows/cd-dotnet-library-publish.yaml@5acd692154f7c4457bbb84636ac366074ddd6273 # v1.19.7
    secrets:
      NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
